{
  "name": "ecom-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch data.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
